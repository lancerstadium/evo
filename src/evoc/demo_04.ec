
mod dm4

enum AnimalType {
    CAT
    DOG
    BIRD
}

struct Dog{
    name : i32
    age : str
    fn eat()
}

fn add(a : i32, b : i32)

fn main(argc : i32, argv : str) {
    1
    fn skd(sdsd : i32, xxx:i64)
    enum LLL{
        MCV
        MSI
    }
    struct Person{
        weight : i32
        height : i32
    }
    a = 23
    if a==x { 
        a = 1 
    } else {
        a
    }
    for hello == x1{
        do_something
    }
    x = -(a + 2) * 3
    return x

}